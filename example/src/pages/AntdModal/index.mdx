import Main from "./Main"
import MainRaw from './Main?raw'
import AntdModalRaw from './AntdModal?raw'
import Preview from "../../components/Preview";

## AntdModal

```tsx
import { Button } from 'antd'
import AntdModal from './AntdModal'

// highlight-start
export default function () {
  const handleClick = () => {
    AntdModal.show({
      demo: {
        a: '1'
      },
      title: 'fd'
    })
  }
  return <Button onClick={handleClick}>展示</Button>
}
// highlight-end
```

<Preview
  files={[
    {
      title: 'Main.tsx',
      code: MainRaw
    },
    {
      title: 'AntdModal.tsx',
      code: AntdModalRaw
    }
  ]}
  code={<Main/>}
/>
