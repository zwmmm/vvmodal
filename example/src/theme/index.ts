import { system } from '@theme-ui/presets'
import { merge } from 'theme-ui'

export default merge(system, {
  colors: {
    modes: {
      dark: {
        muted: '#303030',
        background: '#141414'
      }
    }
  }
})
